# Testing

We use the standard DashKite testing approach. The tests cover the task definitions and ensure the release lifecycle behaves correctly under various conditions.

You can run the tests using the following command:

```bash
npx genie test
```
